\documentclass[12pt]{article}

\usepackage{sbc-template}
\usepackage{graphicx,url}
\usepackage[utf8]{inputenc}
\usepackage[brazil]{babel}
\usepackage{booktabs}
\usepackage{float}
\usepackage{amsmath}
\usepackage{tikz}
\usetikzlibrary{positioning,shapes.geometric,calc,fit,backgrounds}

% ---- Estilos do diagrama de arquitetura (Figura 1) ----
\tikzset{
  sadbase/.style  ={rounded corners=3pt, draw, align=center, inner sep=5pt,
                    font=\footnotesize, minimum height=11mm, text width=40mm},
  sadio/.style    ={sadbase, fill=blue!8,   draw=blue!55,  line width=0.6pt},
  sadproc/.style  ={sadbase, fill=teal!9,    draw=teal!55,  line width=0.6pt},
  sadmodel/.style ={sadbase, fill=violet!8,  draw=violet!60,line width=0.7pt},
  sadout/.style   ={sadbase, fill=orange!12, draw=orange!80,line width=1.0pt, minimum height=9mm},
  sadgrp/.style   ={rounded corners=6pt, draw=black!22, dashed, inner sep=8pt},
  sadgrplbl/.style={font=\scriptsize\bfseries\sffamily, text=black!55},
  sadfl/.style    ={-latex, draw=black!62, line width=0.9pt},
  sadflt/.style   ={-latex, draw=black!40, line width=0.7pt, dashed},
}

\sloppy

\title{Sistema de Recomendação Musical Baseado em Conteúdo para Plataformas de Streaming: Um Protótipo de Apoio à Decisão}

\author{Felipe Lazzarini Cunha, Lucas Castro Carvalho, Pedro Detoni Pereira}

\address{Departamento de Ciência da Computação (DCC)\\
  Universidade Federal de Juiz de Fora (UFJF)\\
  Juiz de Fora -- MG -- Brasil
}

\begin{document}

\maketitle

\begin{abstract}
  With catalogs comprising tens of millions of tracks, music streaming platforms present a classic case of choice overload. In this context, recommendation engines act as essential Decision Support Systems (DSS). This report presents the design and implementation of a content-based music recommendation prototype using the Spotify Tracks Dataset from Kaggle. Leveraging audio signal features (e.g., danceability, energy, and acousticness), we implemented and compared two analytical approaches: Cosine Similarity of user centroids and K-Means Clustering. The prototype was developed in Python with a Streamlit interface. Evaluation was conducted through metrics of diversity (Intra-List Similarity), accuracy (Genre Precision), catalog coverage, and cluster quality (Silhouette Score), demonstrating the feasibility of track-level content recommendation without user-item interaction data.
\end{abstract}

\begin{resumo}
  Com catálogos que abrangem dezenas de milhões de faixas, as plataformas de streaming de música apresentam um caso clássico de sobrecarga de escolhas. Nesse contexto, os motores de recomendação atuam como Sistemas de Apoio à Decisão (SAD) indispensáveis. Este relatório apresenta o desenvolvimento de um protótipo de recomendação musical baseado em conteúdo utilizando o Spotify Tracks Dataset do Kaggle. Aproveitando características físicas de áudio (como dançabilidade, energia e acústica), implementamos e comparamos duas abordagens analíticas: Similaridade do Cosseno do centróide do usuário e Agrupamento via K-Means. O protótipo foi desenvolvido em Python com interface Streamlit. A avaliação foi conduzida por meio de métricas de diversidade (Intra-List Similarity), acurácia (Precisão de Gênero), cobertura de catálogo e qualidade dos clusters (Silhouette Score), demonstrando a viabilidade de recomendação baseada em conteúdo sem histórico de interações.
\end{resumo}

\section{Introdução}
No cenário contemporâneo de consumo digital, as plataformas de streaming de música disponibilizam acervos colossais contendo dezenas de milhões de faixas. Essa abundância, embora positiva, gera uma sobrecarga de informações, tornando a descoberta manual de novos conteúdos uma tarefa inviável para o usuário. Diante deste problema de decisão, os sistemas de recomendação operam como Sistemas de Apoio à Decisão (SAD) essenciais, filtrando o catálogo para sugerir faixas relevantes e alinhadas aos gostos individuais.

Este trabalho apresenta a implementação do protótipo de recomendação musical desenvolvido para o Trabalho de Verificação de Conhecimento (TVC 3). A pergunta-problema que guia o projeto é: \textit{``Como recomendar novas músicas relevantes a um usuário a partir de um conjunto de faixas de sua preferência, usando as características de áudio das músicas?''}. O escopo do projeto é estritamente baseado nas características das faixas (nível de áudio), uma vez que a base de dados não contém histórico de interações de usuários (dados de escuta), o que inviabiliza técnicas de filtragem colaborativa e impõe uma abordagem baseada em conteúdo.

Os objetivos específicos contemplam:
\begin{enumerate}
    \item Representar cada faixa musical por suas características quantitativas de áudio;
    \item Implementar o cálculo de similaridade matemática entre faixas;
    \item Gerar recomendações personalizadas a partir de faixas-semente selecionadas;
    \item Avaliar quantitativamente as recomendações sob as dimensões de diversidade, cobertura e acurácia.
\end{enumerate}

\section{Fundamentação Teórica}
\subsection{Filtragem Baseada em Conteúdo (Content-Based Filtering)}
A filtragem baseada em conteúdo assume que o usuário preferirá itens similares àqueles que ele gostou no passado \cite{lops2011content}. Cada item do catálogo é descrito por um vetor de características (\textit{features}). No caso de música, essas características correspondem a metadados (gênero, artista) e atributos acústicos extraídos do sinal de áudio. 

Diferente de sistemas colaborativos, a recomendação baseada em conteúdo não sofre com o problema de inicialização a frio (\textit{cold-start}) para novos itens e não depende de uma base massiva de usuários ativos, focando exclusivamente nas propriedades intrínsecas das faixas \cite{ricci2015recommender}.

\subsection{Similaridade do Cosseno}
Para determinar o quão próxima uma música candidata está das preferências do usuário, calcula-se a similaridade do cosseno. Dado o vetor do perfil do usuário $\vec{u}$ (calculado como a média dos vetores das faixas-semente) e o vetor da faixa candidata $\vec{v}$, a similaridade do cosseno é o cosseno do ângulo entre os dois vetores, definida por:
\begin{equation}
    \text{Sim}(\vec{u}, \vec{v}) = \frac{\vec{u} \cdot \vec{v}}{\|\vec{u}\| \|\vec{v}\|} = \frac{\sum_{i=1}^{d} u_i v_i}{\sqrt{\sum_{i=1}^{d} u_i^2} \sqrt{\sum_{i=1}^{d} v_i^2}}
\end{equation}
Este valor varia entre $-1.0$ e $1.0$, sendo que no domínio de características normalizadas não-negativas, o intervalo útil situa-se entre $0.0$ (ortogonalidade total, nenhuma similaridade) e $1.0$ (mesma direção e sentido, identidade perfeita).

\subsection{Agrupamento K-Means}
O algoritmo K-Means é uma técnica de aprendizado não supervisionado que particiona $N$ observações em $K$ clusters distintos, onde cada observação pertence ao cluster cujo centróide é mais próximo \cite{macqueen1967some}. No SAD implementado, o K-Means serve como um pré-filtro estilístico: agrupa o catálogo em nichos sonoros. Ao selecionar faixas-semente, o sistema identifica seus clusters correspondentes e ranqueia prioritariamente faixas pertencentes a estes mesmos subgrupos, restringindo o espaço de busca a vizinhanças de maior coesão de áudio.

\section{Metodologia Computacional e Tratamento de Dados}
\subsection{Spotify Tracks Dataset e Tratamento de Dados}
Utilizou-se o dataset real \textit{Spotify Tracks Dataset} extraído do Kaggle \cite{spotify_dataset}, composto originalmente por 114.000 faixas musicais de 125 gêneros e 20 colunas.

A etapa de higienização de dados consistiu em:
\begin{itemize}
    \item Remoção de registros duplicados utilizando como chave o identificador único \texttt{track\_id};
    \item Tratamento de nulos em metadados vitais de visualização (\texttt{track\_name}, \texttt{artists}, \texttt{album\_name});
    \item Limpeza do separador padrão de múltiplos artistas (\texttt{;;}) para formato amigável (\texttt{, }).
\end{itemize}

Como o dataset original contém a mesma faixa replicada em múltiplos gêneros, a deduplicação por \texttt{track\_id} reduziu a base de 114.000 para 89.740 faixas únicas, distribuídas em 113 gêneros efetivamente presentes. Este é o catálogo efetivo utilizado em todos os experimentos.

Após o saneamento, selecionou-se 11 características numéricas do sinal de áudio para compor o espaço de representação vetorial: \textit{danceability}, \textit{energy}, \textit{key}, \textit{loudness}, \textit{mode}, \textit{speechiness}, \textit{acousticness}, \textit{instrumentalness}, \textit{liveness}, \textit{valence} e \textit{tempo}. Como estas variáveis possuem escalas muito distintas (ex: o volume varia em decibéis negativos de $-60$ a $0$, enquanto a acústica varia de $0.0$ a $1.0$), aplicou-se a normalização z-score através do \texttt{StandardScaler} da biblioteca Scikit-Learn \cite{scikit-learn}, garantindo peso equivalente para todas as características no cálculo de distância euclidiana e cosseno.

\subsection{Arquitetura e Fluxo do SAD}
O sistema foi desenvolvido utilizando a linguagem Python, Scikit-Learn (para modelagem e métricas), Pandas/NumPy (manipulação de tensores) e Streamlit para interface visual \cite{streamlit}. O fluxo de processamento de apoio à decisão é sintetizado na Figura~\ref{fig:fluxo_sistema}.

\begin{figure}[htbp]
\centering
\resizebox{0.62\textwidth}{!}{%
\begin{tikzpicture}
% ENTRADA
\node[sadio] (cat)  at (-3.3, 0)   {\textbf{Catálogo Spotify}\\114.000 faixas brutas (Kaggle, CC0)};
\node[sadio] (seed) at ( 3.3, 0)   {\textbf{Faixas-semente}\\1--5 faixas escolhidas pelo usuário};
% DADOS
\node[sadproc] (prep) at (-3.3,-3.4) {\textbf{Pré-processamento}\\Dedup.\ por \texttt{track\_id} $\rightarrow$ 89.740 faixas; z-score (11 \textit{features})};
\node[sadproc] (prof) at ( 0.0,-6.6) {\textbf{Perfil do usuário}\\Centróide $\vec{u}$ = média das faixas-semente normalizadas};
% MOTOR
\node[sadmodel] (cos) at (-3.3,-9.9) {\textbf{Similaridade do Cosseno}\\$\mathrm{sim}(\vec u,\vec v)$ do perfil contra \emph{todo} o catálogo};
\node[sadmodel] (km)  at ( 3.3,-9.9) {\textbf{K-Means} (pré-filtro)\\Cluster majoritário das sementes; cosseno só no cluster};
% SAÍDA
\node[sadproc]  (filt) at (0.0,-13.2) {\textbf{Filtros}\\Exclui as sementes $\cdot$ restrição de gênero (opcional)};
\node[sadout]   (top)  at (0.0,-15.4) {\textbf{Top-N recomendações}};
% SETAS
\draw[sadfl] (cat)  -- (prep);
\draw[sadfl] (seed) -- (prof);
\draw[sadfl] (prep) -- (prof);
\draw[sadfl] (prof) -- (cos);
\draw[sadfl] (prof) -- (km);
\draw[sadfl] (cos)  -- (filt);
\draw[sadfl] (km)   -- (filt);
\draw[sadfl] (filt) -- (top);
% catálogo normalizado alimenta o motor como pool de candidatos
\draw[sadflt] (prep.west) -- ++(-1.1,0) coordinate (p1)
      (p1) |- (cos.west);
\node[font=\scriptsize\itshape, text=black!45, rotate=90, anchor=south]
      at ($(p1)+(-0.15,-3.1)$) {catálogo normalizado (candidatos)};
% RÓTULOS DE ESTÁGIO
\begin{scope}[on background layer]
  \node[sadgrp, fit=(cat)(seed),  label={[sadgrplbl]right:ENTRADA}] {};
  \node[sadgrp, fit=(prep)(prof), label={[sadgrplbl]right:DADOS}] {};
  \node[sadgrp, fit=(cos)(km),    label={[sadgrplbl]right:MOTOR}] {};
  \node[sadgrp, fit=(filt)(top),  label={[sadgrplbl]right:SAÍDA}] {};
\end{scope}
\end{tikzpicture}%
}
\caption{Fluxo de Processamento e Arquitetura do SAD Musical}
\label{fig:fluxo_sistema}
\end{figure}

O fluxo operacional do decisor na plataforma compreende:
\begin{enumerate}
    \item \textbf{Entrada:} O usuário busca faixas no catálogo e adiciona de 1 a 5 faixas-semente.
    \item \textbf{Extração de Perfil:} O sistema extrai e calcula a média das características normalizadas dessas faixas para modelar o centróide de preferência do usuário.
    \item \textbf{Processamento Analítico:} 
    \begin{itemize}
        \item \textbf{Cosseno:} Calcula similaridade entre o perfil e todo o catálogo.
        \item \textbf{K-Means:} Identifica o cluster majoritário das sementes e calcula similaridade apenas com candidatos deste cluster.
    \end{itemize}
    \item \textbf{Filtros e Saída:} Aplica exclusão das sementes, restrições opcionais de gênero (via interface) e retorna a lista ordenada com as Top-N músicas recomendadas.
\end{enumerate}

\subsection{Configuração Experimental e Reprodutibilidade}
Todos os experimentos relatados na Seção~\ref{sec:resultados} são \textbf{integralmente reprodutíveis}. Fixou-se a semente aleatória global em $42$ para todas as etapas estocásticas: o \texttt{StandardScaler} é determinístico; o K-Means foi instanciado com \texttt{random\_state=42} e \texttt{n\_init=10}; e o sorteio de faixas-semente e de consultas aleatórias nos experimentos é realizado por instâncias locais de \texttt{numpy.random.RandomState(42)}. Dessa forma, execuções repetidas produzem exatamente os mesmos números. A Tabela~\ref{tab:hiperparametros} consolida os hiperparâmetros e o ambiente de software utilizados.

\begin{table}[H]
\centering
\caption{Configuração experimental e ambiente de reprodução}
\label{tab:hiperparametros}
\begin{tabular}{ll}
\toprule
\textbf{Parâmetro / Componente} & \textbf{Valor} \\
\midrule
Semente aleatória (\textit{seed}) & 42 \\
Características de áudio & 11 (normalizadas via z-score) \\
Faixas do catálogo (pós-limpeza) & 89.740 \\
$K$ (K-Means, experimentos comparativos) & 10 \\
Precisão de Gênero & 3 sementes, Top-10, 20 repetições \\
Cobertura / ILS & 40 consultas, Top-10 \\
Amostra da silhueta & 2.000 faixas \\
\midrule
Python & 3.12 \\
scikit-learn & 1.9 \\
pandas / NumPy & 2.2 \\
Streamlit / Plotly & 1.59 / 6.2 \\
\bottomrule
\end{tabular}
\end{table}

Para regenerar todas as tabelas desta seção a partir da base bruta, basta posicionar o arquivo \texttt{data/dataset.csv} (obtido no Kaggle \cite{spotify_dataset}) e executar, na raiz do projeto, o comando único \texttt{python reproduzir\_experimentos.py}. O script carrega os dados, treina os modelos e imprime, de forma determinística, os valores reportados a seguir.

\section{Resultados e Discussões}
\label{sec:resultados}
\subsection{Visualização e Interface Streamlit}
A interface gráfica de negócios foi desenhada em páginas estruturadas (\textit{multipage app}) com tema escuro e elementos visuais de alto impacto baseados na biblioteca Plotly, compreendendo três módulos principais:
\begin{enumerate}
    \item \textbf{Explorar Dados:} Painel de análise exploratória interativo contendo indicadores volumétricos do acervo, histogramas de distribuição de variáveis de áudio, mapa de calor de correlações e plot de dispersão bi-dimensional dos gêneros.
    \item \textbf{Recomendador:} Dashboard operacional onde o decisor monta sua playlist de sementes e recebe em tempo real o ranking das Top-N faixas mais similares. Exibe também um gráfico do tipo radar sobreposto, confrontando a assinatura acústica média do usuário com a assinatura acústica do catálogo recomendado.
    \item \textbf{Avaliação:} Painel científico para validação das premissas analíticas.
\end{enumerate}

\subsection{Resultados das Métricas de SAD}
A validação quantitativa da modelagem analítica do SAD musical foi executada por meio de simulações estatísticas determinísticas (\textit{seed}~$=42$) no módulo de avaliação, todas reprodutíveis via \texttt{reproduzir\_experimentos.py}.

\subsubsection{Acurácia por Precisão de Gênero (Genre Precision@10)}
Como a base não contém histórico de interações usuário-item, adotou-se a \textit{Precisão de Gênero} como proxy de acurácia. A partir de 3 faixas-semente de um único gênero, gera-se a recomendação aberta (Top-10 sobre todo o catálogo) e mede-se a fração média --- sobre 20 repetições --- das recomendações pertencentes ao mesmo gênero da semente. A referência é o \textit{baseline} aleatório de $1/113 = 0,88\%$. A Tabela~\ref{tab:genreprec} apresenta os resultados; os valores são idênticos para Cosseno e K-Means, reforçando a equivalência entre os métodos discutida adiante.

\begin{table}[H]
\centering
\caption{Precisão de Gênero@10 por gênero (recomendação aberta, 3 sementes, 20 repetições)}
\label{tab:genreprec}
\begin{tabular}{lrr}
\toprule
\textbf{Gênero avaliado} & \textbf{Precisão@10} & \textbf{Ganho vs.\ acaso} \\
\midrule
Clássica & 25,0\% & 28,2$\times$ \\
Rock     & 9,0\%  & 10,2$\times$ \\
Pop      & 5,5\%  & 6,2$\times$ \\
Metal    & 1,5\%  & 1,7$\times$ \\
Jazz     & 0,5\%  & 0,6$\times$ \\
\bottomrule
\end{tabular}
\end{table}

Os resultados revelam um comportamento fortemente dependente do gênero. Para estilos acusticamente distintivos, como a música clássica, a precisão atinge $25\%$ --- cerca de $28\times$ acima do acaso ---, evidenciando que o espaço de características de áudio carrega sinal de gênero relevante. Contudo, para gêneros situados no ``meio do espectro'' sonoro, como metal e jazz, a precisão cai ao nível do acaso, indicando forte sobreposição acústica com gêneros vizinhos. Este achado é uma evidência direta de que os 113 gêneros finos do dataset \textbf{não são linearmente separáveis} apenas por atributos de áudio, corroborando quantitativamente os valores modestos de silhueta (Seção~4.2.3). Vale ressaltar que o sistema não tem por objetivo reconstruir rótulos de gênero: as recomendações geradas exibem similaridade do cosseno tipicamente superior a $0,90$ em relação ao perfil-semente, evidenciando alta \textit{coesão acústica}, que é a real proposta de valor da filtragem por conteúdo.

\subsubsection{Diversidade (ILS) e Cobertura do Catálogo (Coverage)}
Sob 40 consultas aleatórias gerando 10 recomendações cada, computaram-se a similaridade intra-lista (ILS) e a cobertura de catálogo (Tabela~\ref{tab:ils_cov}).

\begin{table}[H]
\centering
\caption{Comparativo de Diversidade (ILS) e Cobertura do Catálogo}
\label{tab:ils_cov}
\begin{tabular}{lrr}
\toprule
\textbf{Algoritmo Avaliado} & \textbf{Média ILS} & \textbf{Cobertura Global} \\
\midrule
Similaridade do Cosseno & 0,905 & 0,446\% (400 faixas únicas) \\
K-Means Clustering (K=10) & 0,903 & 0,446\% (400 faixas únicas) \\
\bottomrule
\end{tabular}
\end{table}

O resultado mais relevante é a \textbf{equivalência prática entre os dois algoritmos} nesta escala (ILS $\approx 0,90$ e cobertura idêntica). Isso ocorre porque, com sementes distribuídas por todo o catálogo, o cluster majoritário identificado pelo K-Means é tipicamente muito grande (dezenas de milhares de faixas); ao reordenar seus candidatos pela mesma similaridade do cosseno, o K-Means acaba reproduzindo, na prática, o comportamento da busca aberta. O ILS elevado ($0,90$) indica listas internamente coesas (baixa diversidade), coerente com o objetivo de conteúdo. A cobertura baixa em 40 consultas não denota efeito-bolha, mas sim subamostragem: como mostra a Tabela~\ref{tab:coverage_sweep}, a cobertura cresce de forma praticamente linear com o número de consultas, sem saturar, o que confirma uma distribuição de cauda longa saudável.

\begin{table}[H]
\centering
\caption{Cobertura do catálogo em função do número de consultas (Cosseno, Top-10)}
\label{tab:coverage_sweep}
\begin{tabular}{rrr}
\toprule
\textbf{Nº de consultas} & \textbf{Cobertura} & \textbf{Faixas únicas} \\
\midrule
40  & 0,446\% & 400 \\
200 & 2,187\% & 1.963 \\
500 & 5,348\% & 4.799 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Validação de Silhueta para Agrupamento K-Means}
Executou-se o coeficiente de silhueta variando $K$ em uma amostra de 2.000 faixas (Tabela~\ref{tab:silhueta}). O melhor valor foi obtido em $K=8$ ($0,147$), com queda gradual para $K$ maiores.

\begin{table}[H]
\centering
\caption{Coeficiente de Silhueta em função de $K$}
\label{tab:silhueta}
\begin{tabular}{rrrrrrr}
\toprule
$K$ & 5 & 8 & 10 & 12 & 15 & 20 \\
\midrule
Silhueta & 0,146 & \textbf{0,147} & 0,133 & 0,129 & 0,118 & 0,118 \\
\bottomrule
\end{tabular}
\end{table}

Os valores de silhueta, todos entre $0,12$ e $0,15$, indicam uma estrutura de agrupamento \textit{fraca porém real}: existe separação estatística entre grupos acusticamente extremos (ex.: clássica \textit{vs.} eletrônica), mas há forte sobreposição de espectro de áudio entre os 113 gêneros finos (ex.: pop, rock e indie). Esse achado é consistente com a baixa precisão de gênero observada e explica por que o pré-filtro por clusters pouco altera o resultado final em relação à busca aberta.

Cabe justificar o uso de $K=10$ nos experimentos comparativos (Tabelas~\ref{tab:hiperparametros} e \ref{tab:ils_cov}) em vez do ótimo estrito de silhueta em $K=8$. A diferença de coesão entre os dois valores é desprezível ($0,147$ contra $0,133$) e ambos permanecem no mesmo patamar de estrutura fraca; além disso, como demonstrado nesta seção, o desempenho de recomendação é praticamente equivalente ao da busca aberta para qualquer $K$ nessa faixa, pois o cluster majoritário das sementes permanece amplo o bastante para não restringir efetivamente o espaço de busca. Adotou-se, portanto, $K=10$ como valor de referência convencional para o particionamento estilístico, sem prejuízo às conclusões: os resultados são estáveis em todo o intervalo $K\in[8,12]$.

\section{Conclusões}
Este trabalho concluiu com sucesso a implementação prática de um Sistema de Apoio à Decisão voltado para recomendação musical. A modelagem quantitativa empregando Similaridade do Cosseno e K-Means Clustering foi traduzida em um aplicativo Streamlit dinâmico, robusto e com resultados integralmente reprodutíveis.

A validação analítica, conduzida sobre as 89.740 faixas do catálogo real, evidenciou três achados principais. Primeiro, o sistema recupera de forma confiável faixas acusticamente semelhantes ao perfil-semente (similaridade do cosseno tipicamente acima de $0,90$), cumprindo seu objetivo de filtragem por conteúdo. Segundo, na escala do catálogo completo, a Similaridade do Cosseno e o K-Means apresentam desempenho \textit{praticamente equivalente} (ILS $\approx 0,90$ e cobertura idêntica), pois o cluster majoritário das sementes é amplo o suficiente para que o pré-filtro do K-Means pouco restrinja o espaço de busca. Terceiro, a análise de silhueta (ótimo em $K=8$, com valores entre $0,12$ e $0,15$) e a precisão de gênero fortemente dependente do estilo (de $28\times$ o acaso na clássica ao nível do acaso em metal e jazz) revelam que as características de áudio, embora coerentes, não separam de maneira nítida os 113 gêneros finos do dataset.

Como limitações, além do conhecido efeito de bolha sonora inerente à filtragem por conteúdo, aponta-se a forte sobreposição acústica entre gêneros musicais vizinhos, que limita a precisão de gênero para estilos não distintivos. Trabalhos futuros podem investigar abordagens híbridas — combinando características de áudio a redes de co-ocorrência de artistas ou a dados de comportamento do usuário — e o agrupamento dos gêneros finos em macro-gêneros para avaliação de acurácia, além de validação por juízes humanos.

\bibliographystyle{sbc}
\bibliography{sbc-template}

\end{document}
